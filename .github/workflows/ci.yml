name: CI
on:
  pull_request:
  push:
    branches: [main]
jobs:
  ci:
    uses: tds-g3-2s2026/udesa-x-platform/.github/workflows/ci-node.yml@main
    secrets: inherit
    with:
      # The backoffice does deploy a container, so the image gets built and
      # checked. The gate stays off until S5, per ARQUITECTURA.md.
      verificar-imagen: true
